local Service(common) = {
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": common.name,
  },
  "spec": {
    "selector": {
      "app.kubernetes.io/name": common.name,
    },
    "ports": [
      {
        "port": 80,
        "targetPort": "http"
      }
    ]
  }
};

Service
